import 'package:flutter/material.dart';

final glowBoxShadow = [
  BoxShadow(
      color: Colors.black.withOpacity(0.4),
      blurRadius: 6.0,
      spreadRadius: 3.0,
      offset: const Offset(0.0, 3.0))
];
